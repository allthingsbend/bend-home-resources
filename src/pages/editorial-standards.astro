---
import ResourcePage from '../components/ResourcePage.astro';
import { trustPages } from '../lib/content';
const page = trustPages.find((item) => item.slug === 'editorial-standards');
if (!page) throw new Error('Missing content for editorial-standards');
---
<ResourcePage page={page} />
