---
import ResourcePage from '../components/ResourcePage.astro';
import { getTrustPages } from '../lib/content';
const pages = await getTrustPages();
const entry = pages.find((item) => item.data.slug === 'about');
if (!entry) throw new Error('Missing content for about');
---
<ResourcePage entry={entry} />
